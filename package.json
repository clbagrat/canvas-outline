{
  "name": "@canvas-outline/monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --filter='./packages/*' -r build"
  },
  "devDependencies": {
    "typescript": "^4.7.4",
    "vite": "^3.0.5",
    "vite-plugin-dts": "^1.4.1"
  }
}
